---
/**
 * POST CARD — the preview block for one post or writing entry in a list.
 *
 * Used on the homepage, /posts, and /writing. The flags (showHeroImage,
 * showTags, showExcerpt, layout) let each list choose how much to show.
 * Styles live in src/styles/global.css under "Post lists".
 */
import type { PostCardData, WritingCardData } from '../lib/content.types';
import LocalImage from './LocalImage.astro';
import TagList from './TagList.astro';

interface Props {
  post: PostCardData | WritingCardData;
  hrefPrefix?: string;
  showHeroImage?: boolean;
  showTags?: boolean;
  showExcerpt?: boolean;
  layout?: 'stacked' | 'horizontal';
}

const {
  post,
  hrefPrefix = '/posts',
  showHeroImage = true,
  showTags = true,
  showExcerpt = true,
  layout = 'stacked',
} = Astro.props;
const href = `${hrefPrefix}/${post.slug}`;
---

<article
  class:list={[
    'post-card',
    {
      'post-card-no-image': !showHeroImage,
      'post-card-horizontal': layout === 'horizontal',
    },
  ]}
>
  {showHeroImage && (
    <a class="post-card-image-wrap" href={href}>
      <LocalImage
        image={post.heroImage}
        alt={post.heroImageAlt ?? post.title}
        class="post-card-image"
        sizes="(max-width: 980px) 92vw, 520px"
      />
    </a>
  )}

  <div class="post-card-content">
    <h3 class="post-card-title">
      <a href={href}>{post.title}</a>
    </h3>

    {showExcerpt && <p class="post-card-excerpt">{post.excerpt}</p>}

    {showTags && <TagList tags={post.tags} hrefBase={hrefPrefix} />}
  </div>
</article>
